* requests
* Response 객체
* response.status_code
* response.text
* response.headers
* response.url



# 학습 키워드 정리

## 1. 주요 키워드 목록

| 키워드                  | 분류             | 중요도 | 한 줄 설명                                                   |
| ----------------------- | ---------------- | ------ | ------------------------------------------------------------ |
| requests                | 필수 암기 키워드 | 5      | Python에서 HTTP 요청을 쉽게 보내기 위한 대표적인 라이브러리  |
| HTTP 요청               | 필수 암기 키워드 | 5      | 클라이언트가 서버에 데이터를 요청하는 통신 행위              |
| GET 요청                | 필수 암기 키워드 | 5      | 서버로부터 데이터를 가져올 때 사용하는 기본 HTTP 메서드      |
| requests.get()          | 필수 암기 키워드 | 5      | 지정한 URL로 GET 요청을 보내고 Response 객체를 반환하는 함수 |
| Response 객체           | 필수 암기 키워드 | 5      | 서버 응답의 상태 코드, 본문, 헤더, URL 등을 담는 응답 객체   |
| response.status_code    | 필수 암기 키워드 | 5      | 서버 요청 처리 결과를 나타내는 3자리 상태 코드               |
| response.text           | 필수 암기 키워드 | 5      | 서버가 보낸 본문 내용을 문자열 형태로 반환하는 속성          |
| response.headers        | 필수 암기 키워드 | 4      | 응답 데이터의 형식, 서버 정보 등 메타 정보를 담은 헤더       |
| response.url            | 필수 암기 키워드 | 4      | 리다이렉트 이후 최종적으로 요청이 도달한 URL                 |
| status_code 200         | 필수 암기 키워드 | 5      | 요청이 정상적으로 성공했음을 의미하는 상태 코드              |
| status_code 301/302     | 비교 키워드      | 3      | 요청한 주소가 다른 주소로 이동되었음을 의미하는 상태 코드    |
| status_code 400         | 비교 키워드      | 3      | 잘못된 요청을 의미하는 상태 코드                             |
| status_code 401         | 비교 키워드      | 3      | 인증이 필요함을 의미하는 상태 코드                           |
| status_code 403         | 비교 키워드      | 3      | 접근 권한이 없어 요청이 거부되었음을 의미하는 상태 코드      |
| status_code 404         | 비교 키워드      | 4      | 요청한 페이지나 데이터를 찾을 수 없음을 의미하는 상태 코드   |
| status_code 500         | 비교 키워드      | 4      | 서버 내부 오류가 발생했음을 의미하는 상태 코드               |
| urllib                  | 비교 키워드      | 3      | Python 표준 라이브러리의 HTTP 요청 모듈                      |
| 서드파티 라이브러리     | 일반 학습 키워드 | 3      | Python 기본 내장 기능이 아닌 외부에서 설치해 사용하는 라이브러리 |
| pip install requests    | 필수 암기 키워드 | 4      | requests 라이브러리를 설치하는 명령어                        |
| import requests         | 필수 암기 키워드 | 4      | 코드에서 requests 라이브러리를 사용하기 위해 불러오는 구문   |
| pip show requests       | 일반 학습 키워드 | 2      | requests의 버전과 설치 경로를 확인하는 명령어                |
| httpbin.org             | 일반 학습 키워드 | 4      | HTTP 요청과 응답을 테스트할 수 있는 공개 테스트 서버         |
| 응답 본문 Body          | 필수 암기 키워드 | 4      | 서버가 실제로 보내는 HTML, JSON, 텍스트 등의 내용            |
| 응답 헤더 Header        | 필수 암기 키워드 | 4      | 응답 본문 외에 데이터 형식, 서버 정보 등을 담는 부가 정보    |
| Content-Type            | 일반 학습 키워드 | 3      | 응답 본문의 데이터 형식을 알려주는 대표적인 헤더             |
| application/json        | 일반 학습 키워드 | 3      | 응답 본문이 JSON 형식임을 나타내는 Content-Type 값           |
| JSON 문자열             | 일반 학습 키워드 | 4      | API 응답에서 자주 사용되는 문자열 기반 데이터 형식           |
| HTML                    | 일반 학습 키워드 | 3      | 웹페이지 응답 본문에 포함될 수 있는 문서 형식                |
| response.json()         | 일반 학습 키워드 | 3      | JSON 응답을 Python 자료구조처럼 다룰 때 사용하는 메서드      |
| 딕셔너리 형태           | 일반 학습 키워드 | 3      | headers처럼 key-value 구조로 데이터를 표현하는 방식          |
| headers.get()           | 일반 학습 키워드 | 3      | 특정 헤더가 없을 때 오류 없이 값을 안전하게 가져오는 방식    |
| 리다이렉트              | 비교 키워드      | 4      | 요청한 주소가 다른 최종 URL로 자동 이동되는 현상             |
| 요청 성공 여부 확인     | 필수 암기 키워드 | 5      | 본문 처리 전에 status_code를 먼저 확인하는 핵심 습관         |
| 요청 실패 처리          | 필수 암기 키워드 | 4      | 상태 코드가 200이 아닐 때 오류 원인을 확인하는 처리 방식     |
| Request → Response 흐름 | 필수 암기 키워드 | 5      | HTTP 통신에서 요청을 보내고 응답을 확인하는 기본 구조        |
| 웹 크롤링               | 일반 학습 키워드 | 4      | 웹페이지에서 필요한 데이터를 자동으로 수집하는 작업          |
| API 사용                | 일반 학습 키워드 | 4      | 서버가 제공하는 기능이나 데이터를 HTTP 요청으로 활용하는 방식 |
| 자동화 프로그램         | 일반 학습 키워드 | 3      | 반복적인 요청과 처리를 코드로 자동 실행하는 프로그램         |
| 데이터 수집 프로그램    | 일반 학습 키워드 | 3      | 웹이나 API에서 데이터를 가져와 저장·처리하는 프로그램        |

## 2. 필수 암기 키워드

- requests
- HTTP 요청
- GET 요청
- requests.get()
- Response 객체
- response.status_code
- response.text
- response.headers
- response.url
- status_code 200
- pip install requests
- import requests
- 응답 본문 Body
- 응답 헤더 Header
- 요청 성공 여부 확인
- 요청 실패 처리
- Request → Response 흐름

## 3. 비교 키워드

- status_code 301/302
- status_code 400
- status_code 401
- status_code 403
- status_code 404
- status_code 500
- urllib
- 리다이렉트
